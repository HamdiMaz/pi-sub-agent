import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Pi Sub Agent extension entrypoint.
 *
 * Scaffolding only: no tools, commands, events, or sub-agent behavior are
 * registered yet. Future work should register extension capabilities here.
 */
export default function piSubAgent(_pi: ExtensionAPI): void {
	// Intentionally empty until the sub-agent design is implemented.
}
